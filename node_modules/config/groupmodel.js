var mongoose = require('mongoose');

var Schema = mongoose.Schema;

var goupSchema = mongoose.Schema({
    token: String,
    groupName: String,
    groupMembers: Array,
    groupRequests: Array,
    groupAdmin: String
});

mongoose.connect('mongodb://localhost:27017/groupay-backend');

module.exports = mongoose.model('groups', goupSchema);